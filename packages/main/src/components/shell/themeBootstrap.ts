/** Blocking theme bootstrap — must stay in sync with THEME_KEY / app-store:v1 in store.env. */
export const THEME_BOOTSTRAP = `(function(){try{var raw=localStorage.getItem("app-store:v1");var mode="system";if(raw){var parsed=JSON.parse(raw);var entry=parsed&&parsed["ui:theme"];var v=entry&&entry.value;if(v==="dark"||v==="light"||v==="system")mode=v}var resolved=mode==="system"?(matchMedia("(prefers-color-scheme: dark)").matches?"dark":"light"):mode;var root=document.documentElement;if(!root)return;root.classList.remove("light","dark","system");root.classList.add(resolved);if(mode==="system")root.classList.add("system");root.style.colorScheme=resolved}catch(e){try{var root=document.documentElement;if(!root)return;var resolved=matchMedia("(prefers-color-scheme: dark)").matches?"dark":"light";root.classList.add(resolved,"system");root.style.colorScheme=resolved}catch(_){}}})()`;
